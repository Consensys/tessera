# Create docker image with only distribution jar

FROM adoptopenjdk/openjdk11:alpine

ARG TAR_FILE

RUN export=tarfile=${TAR_FILE} && tar -xvzf $tarfile -C /home/runner/work/tessera/dist

ENTRYPOINT ["/home/runner/work/tessera/dist/bin/tessera"]
