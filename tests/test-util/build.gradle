plugins {
    id "java-library"
}

dependencies {
    compileOnly "jakarta.el:jakarta.el-api"
}

compileJava {
    doFirst {
        options.compilerArgs = [
                "--module-path", classpath.asPath,
        ]
        classpath = files()
    }
}
