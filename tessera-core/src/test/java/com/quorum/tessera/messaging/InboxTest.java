package com.quorum.tessera.messaging;

import com.quorum.tessera.data.EncryptedMessageDAO;
import junit.framework.TestCase;
import org.junit.Before;
import org.junit.Test;

import java.util.ServiceLoader;

import static org.assertj.core.api.Assertions.assertThat;

public class InboxTest  extends TestCase {
  //Inbox inbox;


  @Before
  public void setUp() throws Exception {
    //inbox = mock(Inbox.class);
  }


  @Test
  public void testCreate() {
    //EncryptedMessageDAO encryptedMessageDAO = ServiceLoader.load(EncryptedMessageDAO.class).findFirst().get();
    //assertThat(encryptedMessageDAO).isNotNull();
    //Inbox inbox1 = ServiceLoader.load(Inbox.class).findFirst().get();
    //assertThat(inbox1).isNotNull();
  }
}
